###1.请求数量（合并脚本和样式表，css sprites，拆分初始化负载，划分主域）；
###2.请求带宽（开启GZip，精简javascript，移除重复脚本，图像优化）；
###3.缓存利用（使用cdn，使用外部js和css，添加expires头，减少dns查找，配置etag，使ajax可缓存）；
###4.页面结构（样式表放顶部，脚本放底部，尽早刷新文档的输出）；
###5.代码校验（避免css表达式，避免重定向）。
